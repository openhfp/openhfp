# HFP Examples

> **Draft / pre-alpha — to be added alongside Phase 1.**

Golden samples that double as demos, conformance inputs and pilot artifacts. Planned:

- `service-protocol/` — a service report with a repeating list of faults, a photo
  attachment (blob-ref), locked pre-filled fields, and both author and filler signatures.
