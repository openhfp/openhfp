# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Spike B test PKI + signed fixtures — generated, never committed (contains private keys).
# Reproduce with conformance/verify/gen-pki.sh + build-cases.mjs.
conformance/verify/pki/
conformance/verify/cases/
conformance/verify/.tmp/

# Node / JS
node_modules/
dist/
*.tsbuildinfo
.npm/

# Tauri / Filler build output
filler/target/
filler/dist/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local env / secrets — never commit private keys or .p12
*.p12
*.pfx
*.key
.env
.env.*
